// 🎯 Задача №5: Создать новый массив с удвоенными значениями
// Условие:
// Напиши функцию doubleValues(arr), которая принимает массив чисел и возвращает новый массив, где каждое число удвоено (умножено на 2).

// Способ 1: Метод map()
function doubleValuesMap(arr) {
  return arr.map((num) => num * 2);
}

// -----------------------------------------------------

// Способ 2: Цикл for
function doubleValuesFor(arr) {
  let result = []; // ✅ Новый массив
  // ✅ Проходим по всем элементам
  for (let i = 0; i < arr.length; i++) {
    result.push(arr[i] * 2); // ✅ Умножаем на 2 и добавляем
  }
  return result; // ✅ Возвращаем результат
}

// Примеры:
console.log(doubleValuesFor([1, 2, 3])); // [2, 4, 6]
console.log(doubleValuesFor([5, 10, 15])); // [10, 20, 30]
console.log(doubleValuesFor([-3, 0, 3])); // [-6, 0, 6]
console.log(doubleValuesFor([])); // []

// 📊 СРАВНЕНИЕ ДВУХ СПОСОБОВ:
// Способ 1: map() ⭐
// return arr.map(num => num * 2);
// ✅ Плюсы:

// Одна строка
// Функциональный стиль
// Явное намерение (трансформация)
// Стандарт в современном JS

// ❌ Минусы:
// Немного медленнее для огромных массивов

// Способ 2: for
// let result = [];
// for (let i = 0; i < arr.length; i++) {
//   result.push(arr[i] * 2);
// }
// return result;

// ✅ Плюсы:
// Чуть быстрее производительность
// Полный контроль

// ❌ Минусы:
// Многословнее
// Больше места для ошибок


// 🏆 РЕКОМЕНДАЦИЯ:
// Для реальных проектов:
// 🥇 Используй map() — в 95% случаев
// return arr.map(num => num * 2);
// Почему?

// Читаемость
// Меньше кода
// Стандарт индустрии
// Легче поддерживать

// Когда for?
// Очень критичная производительность (массивы >100,000 элементов)
// Сложная логика с break/continue


// 💼 РЕАЛЬНЫЕ КЕЙСЫ — Где используется:
// 1. Обработка данных с сервера:
// Пришли цены в центах, нужно в грн
// const pricesInCents = [100, 250, 500];
// const pricesInRubles = pricesInCents.map(cents => cents / 100);
// // [1, 2.5, 5]

// 2. Работа с API:
// Извлечь только ID из массива объектов
// const users = [{id: 1, name: 'Alice'}, {id: 2, name: 'Bob'}];
// const userIds = users.map(user => user.id);
// // [1, 2]

// 3. Форматирование данных:
// Добавить символ валюты
// const prices = [100, 200, 300];
// const formatted = prices.map(price => `$${price}`);
// // ['$100', '$200', '$300']

// ⚠️ ТИПИЧНЫЕ ОШИБКИ:
// Ошибка 1: Забыть return
// ❌ НЕПРАВИЛЬНО:
// arr.map(num => {
//   num * 2;  // забыли return!
// });
// // Результат: [undefined, undefined, undefined]

// // ✅ ПРАВИЛЬНО:
// arr.map(num => {
//   return num * 2;
// });
// // Или без фигурных скобок:
// arr.map(num => num * 2);

// Ошибка 2: Пытаться изменить исходный массив
// ❌ НЕПРАВИЛЬНО:
// function doubleValues(arr) {
//   for (let i = 0; i < arr.length; i++) {
//     arr[i] = arr[i] * 2;  // изменяем исходный массив!
//   }
//   return arr;
// }

// // ✅ ПРАВИЛЬНО:
// function doubleValues(arr) {
//   return arr.map(num => num * 2);  // новый массив
// }

// 🎯 ИТОГОВАЯ РЕКОМЕНДАЦИЯ:
// В продакшене пиши так:
// const doubleValues = (arr) => arr.map(num => num * 2);
// Кратко, понятно, стандартно! ✨