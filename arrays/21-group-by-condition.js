// 🚀 Задача №21: Сгруппировать элементы по условию
// Реальный кейс: Разделение пользователей на группы, категоризация данных.
// Условие:
// Напиши функцию groupByParity(arr), которая группирует числа по чётности. Возвращает объект с ключами even и odd, где значения — массивы чисел.

function groupByParity(arr) {
  return arr.reduce(
    (result, num) => {
      (num % 2 === 0 ? result.even : result.odd).push(num);
      return result;
    },
    { even: [], odd: [] },
  );
}

// Примеры:
console.log(groupByParity([1, 2, 3, 4, 5, 6]));
// // { even: [2, 4, 6], odd: [1, 3, 5] }

console.log(groupByParity([10, 15, 20]));
// // { even: [10, 20], odd: [15] }

console.log(groupByParity([1, 3, 5]));
// // { even: [], odd: [1, 3, 5] }

console.log(groupByParity([]));
// // { even: [], odd: [] }
