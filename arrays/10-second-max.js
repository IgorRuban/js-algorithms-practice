// 🎯 Задача №10: Найти второй максимальный элемент
// Реальный кейс: Серебряный призёр в рейтинге, второй по цене товар.
// Условие:
// Напиши функцию findSecondMax(arr), которая возвращает второй по величине элемент. Если такого нет — вернуть null.

// Способ 1: Сортировка + уникальные значения
function findSecondMax(arr) {
  const unique = [...new Set(arr)].sort((a, b) => b - a);

  return unique.length < 2 ? null : unique[1];
}

// Понятнее логика
// Универсальный подход

// ----------------------------------------------------------------------

// Способ 2: Два прохода
function findSecondMaxSecond(arr) {
  const max = Math.max(...arr);

  const withoutMax = arr.filter((num) => num !== max);

  return withoutMax.length === 0 ? null : Math.max(...withoutMax);
}

// Быстрее для больших массивов
// Без сортировки

// Примеры:
console.log(findSecondMaxSecond([1, 5, 3, 9, 2])); // 5
console.log(findSecondMaxSecond([10, 10, 5])); // 5 (игнорируем дубликаты)
console.log(findSecondMaxSecond([7])); // null (нет второго)
console.log(findSecondMaxSecond([3, 3, 3])); // null (все одинаковые)
console.log(findSecondMaxSecond([100, 200])); // 100

// // Второе место в рейтинге игроков
// const scores = [1500, 1500, 1200, 1100];
// const silver = findSecondMax(scores);  // 1200

// // Второй по цене товар
// const prices = [999, 1499, 1499, 799];
// const secondPrice = findSecondMax(prices);  // 999
