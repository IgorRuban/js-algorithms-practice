// 🎯 Задача №17: Переместить все нули в конец массива
// Реальный кейс: Организация данных — важные элементы вперёд, нулевые/пустые назад.
// Условие:
// Напиши функцию moveZerosToEnd(arr), которая возвращает новый массив, где все нули перемещены в конец, сохраняя порядок остальных элементов.

function moveZerosToEnd(arr) {
  const nonZeros = arr.filter((num) => num !== 0);
  const zeros = arr.filter((num) => num === 0);

  return [...nonZeros, ...zeros];
}
// [...arr.filter(n => n !== 0), ...arr.filter(n => n === 0)]

// function moveZerosToEnd(arr) {
//   return arr.reduce((result, num) => {
//     num !== 0 ? result.unshift(num) : result.push(num);
//     return result;
//   }, []);
// }

// Тесты:
console.log(moveZerosToEnd([1, 0, 2, 0, 3])); // [1, 2, 3, 0, 0]
console.log(moveZerosToEnd([0, 0, 1, 2])); // [1, 2, 0, 0]
console.log(moveZerosToEnd([1, 2, 3])); // [1, 2, 3]
console.log(moveZerosToEnd([0, 0, 0])); // [0, 0, 0]
console.log(moveZerosToEnd([])); // []

// // Приоритизация задач (невыполненные вперёд)
// const tasks = [
//   {id: 1, completed: false},
//   {id: 2, completed: true},
//   {id: 3, completed: false}
// ];
// // Аналогично: активные вперёд, завершённые назад

// // Сортировка товаров (в наличии вперёд)
// const products = [
//   {name: 'A', inStock: 0},
//   {name: 'B', inStock: 5},
//   {name: 'C', inStock: 0}
// ];
// // Товары в наличии показываем первыми

// // Организация данных перед отправкой
// const values = [10, 0, 20, 0, 30];
// const organized = moveZerosToEnd(values);
// // [10, 20, 30, 0, 0] - важные данные вперёд