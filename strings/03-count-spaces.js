// Задача №3
// Посчитать количество пробелов в строке.

// Пример:
// "Hello world here"

// Результат:
// 2

// Требования
// 1️⃣ Через цикл (for или while)
// 2️⃣ Через методы (split, filter, match, или другой способ)

// 1 способ через цикл for
const numberSpaces = (str) => {
  let count = 0;

  for (let i = 0; i < str.length; i++) {
    if (str[i] === ' ') count += 1;
  }

  return count;
};

// console.log(numberSpaces('Hello world here'));

// 2 способ через цикл while
const numSpacesWhile = (str) => {
  let i = 0;
  let count = 0;

  while (i < str.length) {
    if (str[i] === ' ') {
      count += 1;
    }
    i++;
  }
  return count;
};

// console.log(numSpacesWhile('Hello world here'));

// Пробел — это ' ' (один символ).
// В строках можно сравнивать символы через str[i].
// for и while решают задачу одинаково — отличается только стиль управления циклом.
// Когда структура простая — for предпочтительнее.

// 3 способ чере методы
// 1️⃣ Через split(' ')

// Идея:
// Разделяем строку по пробелу.
// Количество пробелов = количество частей − 1.

const countSpaces = (str) => {
  return str.split(' ').length - 1;
};

// console.log(countSpaces('Hello world here'));

// ⚠ Важно
// Этот способ корректен, если:
// нет двойных пробелов
// нет пробела в начале/конце
// Если строка " Hello world " — результат будет неточным.
// 📌 Метод простой, но не самый надёжный.

// 2️⃣ Через split('') + filter()
// Идея:
// Разбиваем на массив символов.
// Оставляем только пробелы.
// Считаем их количество.

const countSpaceFilter = (str) => {
  return str.split('').filter((char) => char === ' ').length;
};

// console.log(countSpaceFilter('Hello world here'));

// ✔ Надёжно
// ✔ Читаемо
// ✔ Часто используется

// 3️⃣ Через match(/ /g)
// Идея:
// Регулярное выражение ищет все пробелы.
// g — глобальный поиск.
// Возвращается массив найденных совпадений.

const countSpacesMatch = (str) => {
  const matches = str.match(/ /g);
  return matches ? matches.length : 0;
};

console.log(countSpacesMatch('Hello world here'));

// ⚠ Почему нужна проверка?
// Если пробелов нет:
// "Hello".match(/ /g)
// вернёт null.
// Поэтому без проверки будет ошибка.

// 4️⃣ Через reduce()
{
  const newStr = 'Hello world here';

  let str1 = newStr.split('').reduce((acc, el) => {
    if (el === ' ') {
      acc += 1;
    }
    return acc;
  }, 0);

  console.log(str1);
  // reduce() сворачивает массив в одно значение.
  // acc — накопитель.
  // Второй аргумент reduce — начальное значение.
  // Нужно всегда возвращать acc внутри функции.
  // reduce заменяет цикл + счётчик.
}

// split(' ') → простой способ, но может ломаться на двойных пробелах.
// split('').filter() → безопасный универсальный вариант.
// match(/ /g) → самый короткий способ через regex.
// match() возвращает null, если совпадений нет.
// reduce() — универсальный, но чуть сложнее читается.
