// 📝 ЗАДАЧА №23: Flatten (Плоский массив из вложенного)
// Условие:
// Напиши функцию, которая принимает массив с вложенными массивами (любой уровень вложенности) и возвращает "плоский" массив со всеми элементами.

function flatten(arr) {
  const result = [];
  for (let i = 0; i < arr.length; i++) {
    if (!Array.isArray(arr[i])) {
      result.push(arr[i]);
    } else {
      result.push(...flatten(arr[i]));
    }
  }
  return result;
}

// Примеры:
console.log(flatten([1, [2, 3], [4, [5, 6]]])); // [1, 2, 3, 4, 5, 6]
console.log(flatten([1, 2, 3])); // [1, 2, 3]
console.log(flatten([[1], [2], [3]])); // [1, 2, 3]
console.log(flatten([1, [2, [3, [4]]]])); // [1, 2, 3, 4]
