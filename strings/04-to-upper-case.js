// Задача №4
// Преобразовать строку в верхний регистр без использования .toUpperCase()

// Пример:
// "hello world"

// Результат:
// "HELLO WORLD"

// ❗ Ограничения
// 🚫 Нельзя использовать:
// .toUpperCase()
// сторонние библиотеки

// 💡 Подсказка (ключ к решению)
// Нужно понять одну вещь:
// В JavaScript символы имеют коды (Unicode).

// Например:
// 'a' → 97
// 'A' → 65

// Разница:
// 97 - 65 = 32

// 👉 Маленькая буква отличается от большой на 32.
// 🛠 Что тебе пригодится
// charCodeAt()
// String.fromCharCode()

// 🔥 Логика решения
// Для каждого символа:
// Получить код символа
// Проверить — это маленькая буква?
// Если да → вычесть 32
// Превратить обратно в символ
// Добавить в новую строку

// 📌 Условие
// Работаем сначала:

// Алгоритм:
// Создаём пустую строку для результата
// Проходим по каждому символу
// Если символ 'a'-'z' → преобразуем через charCode
// Добавляем символ в результат
// Возвращаем результат

// 1️⃣ Через цикл
const withoutToUpperCase = (str) => {
  let result = '';
  for (let i = 0; i < str.length; i++) {
    if (str.charCodeAt(i) >= 97 && str.charCodeAt(i) <= 122) {
      result += String.fromCharCode(str.charCodeAt(i) - 32);
    } else {
      result += str[i];
    }
  }
  return result;
};
// console.log(withoutToUpperCase('Hello world'));

// charCodeAt() → код символа
// String.fromCharCode() → символ по коду

const withoutToUpperCaseWhile = (str) => {
  let result = '';
  let i = 0;

  while (i < str.length) {
    let code = str.charCodeAt(i);
    if (code >= 97 && code <= 122) {
      result += String.fromCharCode(code - 32);
    } else {
      result += str[i];
    }
    i++;
  }
  return result;
};

// console.log(withoutToUpperCaseWhile('Hello world'));

// Отличие for vs while
// Цикл	                Что меняется
// for	                Счётчик (i) создаётся в заголовке, увеличивается автоматически
// while	              Счётчик создаётся отдельно, увеличивается вручную (i++)
// Логика внутри цикла	Абсолютно идентична: проверка диапазона, добавление символа в result

// 2️⃣ Через методы
// split('') → массив символов
// map() → преобразуем маленькие буквы в заглавные
// join('') → собираем обратно в строку
// reduce() → аккумулируем новую строку, сразу преобразуя символы

const testStr = 'Hello world';
const mapStrTestFn = (str) => {
  return str
    .split('')
    .map((el) => {
      let code = el.charCodeAt(0);

      if (code >= 97 && code <= 122) {
        return String.fromCharCode(code - 32);
      }

      return el;
    })
    .join('');
};
// console.log(mapStrTestFn(testStr));

const reduceStrTestFn = (str) => {
  return str.split('').reduce((acc, el) => {
    let code = el.charCodeAt(0);

    if (code >= 97 && code <= 122) {
      return acc + String.fromCharCode(code - 32);
    }
    return acc + el;
  }, '');
};

console.log(reduceStrTestFn(testStr));

// 1️⃣ Цикл for
// Перебираем каждый символ
// Проверяем код символа (charCodeAt)
// Преобразуем в заглавную (String.fromCharCode)
// Складываем в строку

// 2️⃣ Цикл while
// Логика та же, что и в for
// Индекс управляется вручную

// 3️⃣ Методы массивов (split + map + join)
// split('') → массив символов
// map() → преобразуем маленькие буквы в заглавные
// join('') → собираем обратно в строку

// 4️⃣ (продвинутый) reduce
// split('') → массив
// reduce() → аккумулируем новую строку, сразу преобразуя символы
// Возвращаем результат

// 📌
// for / while → императивный стиль (тело цикла)
// map / reduce → функциональный стиль (создание нового массива → результат)
// Все решения используют один и тот же принцип: код символа + преобразование через 32

// 🟢 map
// Что делает:
// Преобразует каждый элемент → возвращает массив той же длины.
// Ты думаешь так:
// “Как изменить каждый элемент?”
// ['h','i'] → map → ['H','I']

// ✔ 1 вход → 1 выход
// ✔ Длина массива сохраняется
// ✔ Нет накопления

// 🔵 reduce
// Что делает:
// Сворачивает массив → в одно значение.

// Ты думаешь так:
// “Как собрать всё в один результат?”
// ['h','i'] → reduce → "HI"

// ✔ Есть acc (накопитель)
// ✔ Каждый шаг добавляет к результату
// ✔ Можно получить что угодно: строку, число, объект
