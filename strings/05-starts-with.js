// 5️⃣ Проверить, начинается ли строка с определённого символа
// (без использования .startsWith())

// 🔎 Что нужно реализовать
// Функция должна:
// принимать строку
// принимать символ
// возвращать true или false

// Пример:
// ("Hello", "H") → true
// ("Hello", "h") → false

// 💡 Способы (минимум 2)
// 1️⃣ Через прямое сравнение индекса
// 2️⃣ Через цикл
// 3️⃣ Через методы (charAt, slice)

// 🔹 Подсказка (без готового кода)
// Самая простая логика:
// Первый символ строки — это индекс 0
// Нужно сравнить его с переданным символом

const startsWith = (str, char) => {
  return str[0] === char;
};

// console.log(startsWith('Hello', 'H'));
// console.log(startsWith('Hello', 'h'));
// Первый символ строки всегда доступен через индекс 0
// Сравнение === сразу возвращает boolean

// 2️⃣ способ (charAt или slice).
// 🔹 Подсказка
// charAt(0) → возвращает первый символ
// slice(0, 1) → возвращает подстроку из первого символа
// Сравни с char и верни результат

const startsWithCharAt = (str, char) => {
  return str.charAt(0) === char;
};

// console.log(startsWithCharAt('Hello', 'H'));
// console.log(startsWithCharAt('Hello', 'h'));
// ✔ charAt(0) возвращает первый символ строки
// ✔ Работает так же, как str[0]
// ✔ Возвращает строку длиной 1

const startsWithSlice = (str, char) => {
  return str.slice(0, 1) === char;
};

// console.log(startsWithSlice('Hello', 'H'));
// console.log(startsWithSlice('Hello', 'h'));
// ✔ slice(0, 1) берёт подстроку от 0 до 1 (не включая 1)
// ✔ То есть возвращает первый символ
// ✔ Подходит, если проверяем не 1, а несколько символов

// Если бы проверяли не символ, а слово:
// ("Hello world", "Hell")
// Тогда slice(0, char.length) был бы самым правильным вариантом.
