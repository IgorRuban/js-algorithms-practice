// ✅ Задача №1
// Найти длину строки без использования .length
// Пример:
// "hello" → 5
// 🔹 Требования
// Сделай 2 способа:

// 1️⃣ Через цикл for
// Подсказки:
// заводишь счётчик
// проходишь по строке
// увеличиваешь счётчик вручную
// никаких .length
// Подумай:
// Как понять, что строка закончилась, если .length запрещён?

// 2️⃣ Через методы
// Подсказки:
// .charAt()
// проверка на undefined
// либо другой способ без прямого использования .length

// 🚫 Нельзя:
// использовать .length
// использовать готовые методы типа .split('').length

// Алгоритм:
// Инициализируем счётчик = 0
// Проходим по каждому символу
// Увеличиваем счётчик на 1 за каждый символ
// Возвращаем счётчик

// 1 способ через цикл for
const str = ' hello ';

const stringLength = (str) => {
  let count = 0;

  for (let i = 0; str[i] !== undefined; i++) {
    count++;
  }

  return count;
};

// console.log(stringLength(str));

// Строка в JS ведёт себя как массив символов.
// Если обратиться к индексу вне строки → получим undefined.
// Можно использовать str[i] !== undefined как условие остановки.
// Не нужно дублировать проверки внутри цикла.

// 2 способ через метод charAt()
const strLength = (str) => {
  let count = 0;

  for (let i = 0; str.charAt(i); i++) {
    count++;
  }

  return count;
};

// console.log(strLength(str));

// charAt(i) возвращает '', если индекс вне строки.
// Пустая строка — falsy.
// Можно использовать это как условие остановки цикла.
// Функции возвращают данные, не текст.
// Названия переменных должны отражать смысл.

// 🔹 1 способ — через str[i]
// for (let i = 0; str[i] !== undefined; i++)

// Как работает
// Строка ведёт себя как массив.
// При выходе за границы получаем undefined.
// Мы явно проверяем это.

// Плюсы
// ✔ Быстрее (минимальные операции)
// ✔ Чаще используется в реальном коде
// ✔ Более «низкоуровневый» подход
// ✔ Понятнее, что происходит

// Минус
// Нужно помнить, что строки индексируются как массив.

// 🔹 2 способ — через charAt(i)
// for (let i = 0; str.charAt(i); i++)

// Как работает
// charAt(i) возвращает символ.
// Если индекс вне строки → возвращает '' (пустую строку).
// Пустая строка — falsy → цикл останавливается.

// Плюсы
// ✔ Безопасный старый способ (работает везде)
// ✔ Явно метод строки

// Минус
// Чуть медленнее (метод вызывается каждый раз)
// Менее читаемо
// Реже используется в современном коде

// ⚔ Главная разница
// str[i]	                                        charAt(i)
// Возвращает undefined вне диапазона              	Возвращает ''
// Современный стиль	                              Старый стиль
// Быстрее	                                        Медленнее
// Используется чаще	                              Почти не используется

// В реальной разработке:
// 👉 90% случаев — используют str[i]
// 👉 charAt() ты должен знать, но использовать редко

// 🎯 Вывод
// Оба решения правильные.
// Но первый — более «профессиональный».
