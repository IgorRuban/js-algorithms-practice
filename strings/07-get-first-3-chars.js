// 7️⃣ Получить первые 3 символа строки
// (без использования .slice() в первом способе)

// 🔹 Пример
// "JavaScript" → "Jav"

// 💡 Нужно сделать 2–3 способа:
// 1️⃣ Через for
// 2️⃣ Через while
// 3️⃣ Через методы (slice, substring)

// 🔎 Подсказка для цикла
// Создаём пустую строку
// Проходим от 0 до 3
// Добавляем символы к результату
// Не выйти за границы строки (если строка короче 3 символов)

// Алгоритм:
// Создаём пустую строку
// Проходим по первым 3 символам (или меньше, если строка короче)
// Добавляем символы в результат
// Возвращаем результат
// Альтернатива: slice(0, 3) или substring(0, 3)

// 1️⃣ Через for
const firstThreeCharactersFor = (str) => {
  let char = '';

  for (let i = 0; i < 3 && i < str.length; i++) {
    char += str[i];
  }

  return char;
};

// console.log(firstThreeCharactersFor('JavaScript'));

// 2️⃣ Через while
const firstThreeCharactersWhile = (str) => {
  let char = '';
  let i = 0;

  while (i < 3 && i < str.length) {
    char += str[i];
    i++;
  }

  return char;
};

// console.log(firstThreeCharactersWhile('JavaScript'));

// 🧠 Как работает условие
// i < 3 → максимум 3 символа
// i < str.length → не выйти за пределы строки
// Если строка "Hi":
// длина = 2
// цикл выполнится 2 раза
// получим "Hi"
// без undefined ✅

// 3️⃣ Через методы (slice, substring)
console.log('JavaScript'.slice(0, 3));
console.log('Hello'.substring(0, 3));
// slice	Поддерживает отрицательные индексы
// substring	Отрицательные значения превращает в 0